<h2 class="mt-3">New book</h2>
<%= form_with model: book_new, local:true do |f| %> <!--@book_new-->
 <h4>Title</h4>
 <%= f.text_field :title %>
 <h4>Opinion</h4>
 <%= f.text_area :body %>
 <%= f.submit "Create Book" %>
<% end %>